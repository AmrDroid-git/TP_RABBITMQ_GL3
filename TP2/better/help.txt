The following comments are in the Tunisian dialect. 
Just one remark :
    All provided commands are for Windows.
    Linux's are the same, just change ";" with ":".


Bel falle9i:

//awel 7eja t5adem el docker file bech tasna3 el db's
docker compose up -d


//t3abbi el databases
docker exec -i branch_office_1_db mysql -uroot -proot bo1_sales_db < bio1.sql
docker exec -i branch_office_2_db mysql -uroot -proot bo2_sales_db < bio2.sql
docker exec -i head_office_db mysql -uroot -proot ho_sales_db < ho.sql
//ken t7eb ta3mal talla 3al database content:
docker exec -it branch_office_1_db mysql -uroot -proot bo1_sales_db -e "SELECT * FROM product_sales;"
docker exec -it branch_office_2_db mysql -uroot -proot bo2_sales_db -e "SELECT * FROM product_sales;"
docker exec -it head_office_db mysql -uroot -proot ho_sales_db -e "SELECT * FROM product_sales;"


//tcompili el codes mte3ek:
javac -cp ".;../lib/*" *.java

//t5addem el setup marra fi 7yetek bech traka7 el server
java -cp ".;../lib/*" SetupRabbitMQ

//t5addem el generators of data on separate terminals
java -cp ".:lib/*" DataGeneratorBO1
java -cp ".:lib/*" DataGeneratorBO2

//t5addam eli t7eb 3lih tawa jawk behi
java -cp ".;../lib/*" BranchOffice1
java -cp ".;../lib/*" BranchOffice2
java -cp ".;../lib/*" HeadOffice


//ken t7eb ta3mal talla 3al database content:
docker exec -it branch_office_1_db mysql -uroot -proot bo1_sales_db -e "SELECT * FROM product_sales;"
docker exec -it branch_office_2_db mysql -uroot -proot bo2_sales_db -e "SELECT * FROM product_sales;"
docker exec -it head_office_db mysql -uroot -proot ho_sales_db -e "SELECT * FROM product_sales;"
